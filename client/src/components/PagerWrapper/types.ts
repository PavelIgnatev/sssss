export type PagerModel = {
  state: any;
  prevState: {
    network: string;
    level: string;
    currency: string;
    bid: string;
    status: string;
    name: string;
    ability: string;
  }[];
};
